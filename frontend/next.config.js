/** @type {import('next').NextConfig} */
const nextConfig = {
  // Output standalone build for easy Railway/Docker deployment later
  // output: 'standalone',  // Uncomment when deploying to Railway
};

module.exports = nextConfig;
